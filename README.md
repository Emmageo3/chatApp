# chatApp

I-Base de données
  *Utilisateurs
    -id
    -pseudo
    -mot de passe
    -photo de profil
  *messages
    -timestamp
    -contenu
    -user_id
  *reactions
    -message_id
    -emoji_id
